# Database (SQLite for local dev; switch to Postgres on Vercel)
DATABASE_URL="file:./dev.db"


# Secure Apify → TalentScout ingest webhook
INGEST_TOKEN="set-a-long-random-token"


# Optional basic session cookie (dev mode). Replace with NextAuth later if desired.
SESSION_SECRET="replace-me"
