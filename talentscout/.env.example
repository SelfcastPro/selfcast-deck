# Required token used to validate incoming ingestion webhooks.
# This must match the token configured in the Apify webhook header.
INGEST_TOKEN=

# Apify dataset configuration used by the TalentScout board UI.
# Replace the dataset ID and token with the values from Apify.
APIFY_DATASET_ID=
APIFY_TOKEN=
