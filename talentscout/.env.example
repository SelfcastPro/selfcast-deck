# Required token used to validate incoming ingestion webhooks.
# This must match the token configured in the Apify webhook header.
INGEST_TOKEN=

# Optional: reserve for future authenticated Apify API access.
APIFY_TOKEN=
